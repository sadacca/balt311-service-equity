# Copy this file to secrets.toml and fill in your values.
# NEVER commit secrets.toml — it is gitignored.
# On Streamlit Community Cloud, set these via the Secrets manager in app settings.

[mapbox]
token = "pk.your-mapbox-token-here"
